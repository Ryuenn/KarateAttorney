# Copy to .env and fill in real values. Every variable used by the site is
# documented here and in README.md.
#
# (No variables yet — the speaker-form slice introduces RESEND_API_KEY,
# TURNSTILE keys, notification inbox, and the CRM webhook URL.)
